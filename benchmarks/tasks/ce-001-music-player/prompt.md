Implement the `MusicPlayer` class in `solution.py`.

This is a class as a music player that provides to play, stop, add songs, remove songs, set volume, shuffle, and switch to the next or previous song.

## Methods to implement

- `add_song`: def add_song(self, song):
- `remove_song`: def remove_song(self, song):
- `play`: def play(self):
- `stop`: def stop(self):
- `switch_song`: def switch_song(self):
- `previous_song`: def previous_song(self):
- `set_volume`: def set_volume(self, volume):
- `shuffle`: def shuffle(self):

Tests are in `test_solution.py`.
Run with: `python3 -m pytest test_solution.py -v`

Do not modify test_solution.py.
